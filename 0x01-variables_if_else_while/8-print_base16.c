#include <stdio.h>

/**
* main - the will print hexadecimalks in lower case
* Return: will be 0 (success)
*/

int main(void)
{
int a;
char s;
for (a = 0; a <= 9; a++)
for (s = 'a' ; s <= 'f' ; s++)
putchar(a + 0);
putchar('s');
putchar('\n');
return (0);
}
