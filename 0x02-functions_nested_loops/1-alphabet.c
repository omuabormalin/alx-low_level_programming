#include <main.h>

/**
* main - this is to print alphabets in lower cases
* Return: will be 0 (success)
*/

void print_alphabet_x10(void)
{
char s;
for (s = 1; s <= 27; s++)
{
putchar( s);
}
putchar('\n);
}
