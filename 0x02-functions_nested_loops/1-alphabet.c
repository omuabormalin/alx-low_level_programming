#include <stdio.h>

/**
* main - this is to print alphabets in lower cases
* Return: will be 0 (success)
*/

int print_alphabet(void);
{
char s;
for (s = "a" ; s <= "z" ; s++)
{
putchar("%s\"n, s);
}
return (0);
}
